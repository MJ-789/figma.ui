[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    -v
    -s
    -p no:playwright
    --html=reports/html/report.html
    --self-contained-html

markers =
    desktop: 桌面端测试
    crawl: 页面发现与爬取测试
    element: 结构化元素属性级对比测试（v1.2.0）
