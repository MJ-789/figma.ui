[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    -v
    -s
    -p no:playwright
    --html=reports/html/report.html
    --self-contained-html

markers =
    desktop: 桌面端测试
    cross_browser: 跨浏览器测试
    crawl: 页面发现与爬取测试
